# Python Calculator:

This is a simple command-line calculator program written in Python.

# What it does:
Performs basic arithmetic operations: addition, subtraction, multiplication, and division
Allows continuous calculations using the previous result
Lets the user restart a new calculation anytime

# Concepts Used:
Functions
Dictionary (to map operators with functions)
Loops (while)
Conditional statements (if-else)
User input handling
